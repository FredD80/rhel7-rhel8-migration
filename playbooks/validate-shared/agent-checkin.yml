---
- name: Validate required agent check-in
  hosts: rhel8_targets
  become: true
  gather_facts: false
  tasks:
    - name: Not implemented - agent check-in validation
      ansible.builtin.fail:
        msg: "TODO: validate required management, logging, monitoring, and security agent check-in."
