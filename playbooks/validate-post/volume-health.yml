---
- name: Validate post-cutover volume health
  hosts: rhel8_targets
  become: true
  gather_facts: false
  tasks:
    - name: Not implemented - volume health validation
      ansible.builtin.fail:
        msg: "TODO: validate mounts, filesystem metadata, sizes, and expected data-volume state."
